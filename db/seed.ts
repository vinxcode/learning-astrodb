import { db, User } from 'astro:db'

 export default async function seed() {
	// await db.insert(User).values([
		// { userId: '1', name: "Eduardo", phone: "78787878"},
		// { userId: '2', name: "Marvin", phone: "71898078"}
	// ]);
	 }
